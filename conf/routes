# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Signalement API
POST         /api/signalement                           controllers.SignalementController.createSignalement()

GET         /api/companies/:name/:city                  controllers.EntrepriseController.getCompanies(name, city, maxCount: Int = 10, postcode: Option[String])