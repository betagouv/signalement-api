# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Signalement API
POST         /api/signalement                       controllers.SignalementController.createSignalement()

GET         /api/companies/:search                  controllers.EntrepriseController.getCompanies(search, maxCount: Int = 10)